{
  "DATABASE_URL": "${{Postgres-TEST.DATABASE_URL}}",
  "REDIS_URL": "${{Redis-TEST.REDIS_URL}}",
  "OPENAI_API_KEY": "${{web.OPENAI_API_KEY}}",
  "MOONSHOT_API_KEY": "${{web.MOONSHOT_API_KEY}}",
  "USE_MOONSHOT": "true",
  "ENCRYPTION_KEY": "${{web.ENCRYPTION_KEY}}",
  "SECRET_KEY": "${{web.SECRET_KEY}}",
  "AWS_ACCESS_KEY_ID": "${{web.AWS_ACCESS_KEY_ID}}",
  "AWS_SECRET_ACCESS_KEY": "${{web.AWS_SECRET_ACCESS_KEY}}",
  "AWS_REGION": "${{web.AWS_REGION}}",
  "LAMBDA_FUNCTION_ARN": "${{web.LAMBDA_FUNCTION_ARN}}",
  "MINIMAL_LOGGING": "${{web.MINIMAL_LOGGING}}",
  "GOOGLE_CREDENTIALS_JSON": "${{web.GOOGLE_CREDENTIALS_JSON}}",
  "OAUTH_REDIRECT_URI": "${{web.OAUTH_REDIRECT_URI}}"
}

